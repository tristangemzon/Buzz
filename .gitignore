node_modules
out
dist
.DS_Store
*.log
.env
.env.local
release

# Local SQLCipher artefacts (WAL/SHM from dev runs)
*.sqlite-*

# buzz-mesh sidecar — compiled binaries (run `make all` to regenerate)
buzz-mesh/dist/

# Cloudflare Worker local deps
mesh-key-worker/node_modules/

